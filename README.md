# py scripts

A repository of standalone custom python scripts for handling common files in bioinformatics: FASTA, .h5ad, gtf/gff.

Following standard guidelines with boilerplate design, docstrings, main entry point, module guard, 99% standard library if possible, etc. Soon: black/pylint compliant, venv with requisites.

If due, credit is given. (e.g. an answer from biostars, stack overflow, or similar).
